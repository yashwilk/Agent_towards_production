# Optional overrides - these defaults work with a standard local Ollama install.
# The model must support tool-calling (e.g. qwen2.5:7b, llama3.1:8b, mistral:7b-instruct).
OLLAMA_MODEL=qwen2.5:7b
OLLAMA_BASE_URL=http://localhost:11434
